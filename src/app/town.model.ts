export class Town {
  constructor(public name: string, public radiation: number, public population: number, public danger: number, public stuff: string[], public importantPeople: string[], public id: number){}
}
